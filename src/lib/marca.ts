/**
 * Nome da marca num módulo só (04-ui.md §2.1). Consumido por `metadata`, tela
 * de entrada, rodapé e microcopy — trocar a grafia é editar este arquivo.
 *
 * Pendência P-01 (00-visao.md): o cliente ainda não confirmou "Merlo Store" ou
 * "Merlos Store". Vale "Merlo Store", que é o que está no `marca.json` dele.
 */

/** Wordmark e lugares apertados (cabeçalho, celular). */
export const NOME_CURTO = "Merlo";

/** Nome por extenso: tela de entrada, rodapé, e-mail. */
export const NOME_COMPLETO = "Merlo Store";

/** Título da aba e `metadata.title` do layout raiz. */
export const TITULO_ABA = "Merlo Store — Atendimento";
