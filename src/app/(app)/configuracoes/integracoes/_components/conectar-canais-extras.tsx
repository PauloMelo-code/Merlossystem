/**
 * COSTURA — dono: R2-D (spec r2/final-r2d-canais-extras.md §7).
 * Cartões de conexão do Messenger e do TikTok. Página de M5, arquivo do R2-D.
 * Server component: busca a disponibilidade pela action `lerCanaisExtras`.
 * Até o pacote não mostra nada (sem backend seria fachada).
 */
export async function ConectarCanaisExtras(_props: { lojas: readonly { id: string; nome: string }[] }) {
  return null;
}
