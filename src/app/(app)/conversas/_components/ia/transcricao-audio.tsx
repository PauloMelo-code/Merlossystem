"use client";

import type { MidiaDeAudio, SituacaoDoProvedorIa } from "@/lib/inteligencia/tipos";

/** COSTURA — dono: R2-C. Transcrição abaixo do áudio de entrada (spec r2/final-r2c-inteligencia.md §7). */
export function TranscricaoAudio(_props: { midia: MidiaDeAudio; podePedir: boolean; estado: SituacaoDoProvedorIa }) {
  return null;
}
