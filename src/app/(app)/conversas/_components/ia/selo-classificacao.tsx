import type { IaDaConversa } from "@/lib/inteligencia/tipos";

/** COSTURA — dono: R2-C. Selo da classificação automática; sem estado, seguro no servidor. */
export function SeloClassificacao(_props: { ia: IaDaConversa | null; compacto?: boolean }) {
  return null;
}
