
### M1 — Atendimento e canais — ok=false
commits: 6225cf3 feat(conversas): adaptadores de canal e ingestão de mensagens; fc55423 feat(conversas): inbox, linha do tempo e composer; 161cff0 feat(conversas): tempo real por SSE com reconciliação; 9bf57c9 fix(conversas): reenvio com job próprio e leituras em sequência na transação; 8284b86 docs(conversas): documentar o módulo conversas; 768203f test(conversas): cobrir parsers, ingestão, envio, reenvio…
  FALHA: tests/integracao/contato-upsert.test.ts › 'carimba o whatsapp_id no contato do CRM em vez de criar outro' FALHA: upsertContatoPorCanal (src/lib/db/mutacoes.ts, arquivo da fundação) relança a colisão com uq_contatos_telefone porque violacaoDeTelefone olha e.code/e.constraint, e o Drizzle 0.45 coloca o erro do pg em e.cause. A ingestão do M1 contorna isso (passo 2 antes, por atualizarComTrava) e o teste de ingestão com contato do CRM passa, mas a própria função segue com defeito.
  FALHA: Fumaça no navegador: o console tem centenas de violações de CSP em dev (script inline e eval sem nonce, política report-only) e o aviso '1 Issue' do Next. Vêm da borda/CSP da fundação, não do M1.
  BLOQUEIO: src/lib/db/mutacoes.ts: violacaoDeTelefone() precisa olhar também (erro as {cause}).cause?.code e .constraint (Drizzle 0.45 embrulha o erro do pg). Sem essa correção, o passo 2 de upsertContatoPorCanal nunca roda e o teste contato-upsert fica vermelho.
  BLOQUEIO: src/lib/db/mutacoes.ts: registrarProcessamentoEvento() é citada no plano (§6 M5, 01-dados.md §6.4) mas não existe. O M1 criou a sua versão em src/lib/conversas/_gravacao.ts, sobre atualizarEstado.
  BLOQUEIO: ACOES_AUDITADAS (src/lib/db/schema/_enums/auditoria.ts + CHECK da migração): faltam mensagem_recebida, conversa_criada e conversa_arquivada. Hoje a entrada grava 'mensagem_enviada', a criação de conversa grava 'conversa_reaberta' (com antes=null) e arquivar grava 'conversa_resolvida' com status arquivada no diff. O mapeamento fica em src/lib/conversas/_sistema.ts. Precisa de migração 0018 e ADR.
  BLOQUEIO: ATOR_SISTEMA (03-arquitetura.md §6.4) não existe (sem constante e sem linha em usuarios). Worker e webhook gravam modified_by e ator_id nulos (ator_tipo 'sistema'), com cast em _sistema.ts. O M3 fez o mesmo.
  BLOQUEIO: src/lib/db/listas-fechadas.ts: externo_id não está em ESTADOS_DE_SISTEMA.conversas_mensagens. O id do provedor, depois do envio, é gravado por atualizarComTrava (trilha 'mensagem_enviada'), apesar de a tabela ser exceção da trava de colisão.
  BLOQUEIO: src/lib/db/schema/conversas/mensagens.ts: MetadadosMensagem não declara 'modelo' {template_id, variaveis} (usado para o worker enviar o template) nem 'enviada_pelo_aparelho' (deMim). O jsonb aceita; o tipo fechado não.
  BLOQUEIO: Costura faltando com o M3: não há leitura do binário de lojas_midias para o M1 enviar mídia. Além disso, src/lib/rede/buscarExterno.ts só aceita corpo string, o que impede o upload multipart da Graph (WhatsApp oficial e Instagram). Por isso o composer não tem botão 'Anexar' e mensagem de mídia de saída falha com motivo explícito.
  BLOQUEIO: src/lib/actions/_base.ts: ConfigAcao não aceita renovaAtividade: false. A action resumoDoAtendimento (polling de degradação) renova a atividade da sessão, e a inatividade de 60 min não chega para quem fica em polling.
  BLOQUEIO: docs/seguranca/caminhos-de-acesso.md: /conversas, /conversas/[id] e /api/eventos continuam como 'pacote M1'; precisam virar 'entregue' na integração. A rota '/' (redireciona para /conversas) está atribuída ao M1 no documento, mas src/app/page.tsx não está na minha coluna 'cria e é dono' e não foi criada.
  BLOQUEIO: Divergência de nomes: 04-ui.md cita ActionS de etiquetar e exportar/eliminar dados do titular no menu da conversa. Elas são do M2 e import cruzado é proibido: o menu leva à ficha do contato (/contatos/[id]) e o painel mostra só o selo de opt-out. A integração decide se liga os componentes do M2 aqui.
  BLOQUEIO: Dependência do M5 assumida: chaves da credencial decifrada iguais a catalogo-provedores.ts (whatsapp_oficial: access_token, com referencia_externa = phone_number_id; instagram: page_access_token; uazapi: token). O job processar-evento aceita integracaoId opcional e, sem ele, usa lojas_integracoes_eventos.integracao_id.
  BLOQUEIO: Contador 'N sem resposta' do topo da lista vem do servidor e só se atualiza na navegação (limitação conhecida, registrada no doc do módulo).

### M2 — Contatos, consentimento e LGPD — ok=true
commits: 623721b feat(contatos): carteira por loja, etiquetas e ficha do contato · 582f7b1 feat(lgpd): dossiê do titular, consentimento e anonimização em transação · c40e754 docs(contatos): documentar o módulo contatos · 56f12f4 test(contatos): cobrir carteira, consentimento, dossiê e anonimização LGPD
  FALHA: npm run lint vermelho só por arquivos do M1: conversas/_components/tempo-real.tsx:48 e composer.tsx:70
  FALHA: npm run test:travas: T25 reprova tests/integracao/auditoria-leituras.test.ts:31 (M8)
  FALHA: npm run test:componentes: 11 falhas em tokens.test.ts, em arquivos de auditoria, integracoes, relatorios, pedidos, conversas e galeria (outros pacotes)
  FALHA: npm run test:integracao (m2): integridade-trilha acha 4 órfãos em consentimentos gravados por testes de outros pacotes (campanhas-disparo e relatorios-indicadores gravam em consentimentos); T13 cookie acusa src/app/api/eventos/route.ts (M1) e src/lib/campanhas/pagina.ts (M6); T13 'consulta sem escopo' falhou (a saída não mostra quais arquivos; os módulos do M2 passam); T17 acusa src/lib/usuarios/trocas-email.ts (M7)
  BLOQUEIO: src/lib/db/mutacoes.ts (fundação): não há helper para as atualizações em lote da anonimização (mensagens, prévia, mídias, pesquisa) nem para gravar em consentimentos. Segui o caminho que o gravador da trilha e auth/convites.ts já usam: tx.execute(sql) em src/lib/lgpd/_anonimizacao.ts e src/lib/lgpd/consentimento.ts, com a justificativa no próprio arquivo. A trava de mutações passa, mas pela letra; o espírito dela pede um helper nomeado, por exemplo anonimizarTitular() em mutacoes.ts.
  BLOQUEIO: Achado de LGPD: conversas.ultima_mensagem_previa guarda o texto da mensagem e não está no §8 de 01-dados-dominio. Sem limpá-la, o telefone citado numa mensagem sobreviveria na lista de conversas e o aceite (telefone ausente de todas as tabelas) falharia, por isso a anonimização grava o marcador nela. Pelo mesmo motivo limpo lojas_midias.nome_original das mídias recebidas. Os dois precisam entrar no §8.
  BLOQUEIO: Outros depósitos de PII que o §8 não cobre e que eu NÃO alterei: pedidos.endereco_entrega e pedidos.observacoes (o documento manda manter o pedido), e conversas_agendamentos.conteudo. Precisa de decisão.
  BLOQUEIO: src/lib/db/schema/_enums/auditoria.ts (CAMPOS_PII): falta lgpd_solicitacoes.motivo. inserirAuditado grava o motivo, texto livre, no diff da trilha; a tela avisa para não escrever dado pessoal ali, mas o certo é incluir o campo em CAMPOS_PII.
  BLOQUEIO: Costura com o M8: a carga do job manutencao/limpar-midia que o M2 enfileira é { lojaId, solicitacaoId, midiaIds }, com jobId 'lgpd-<solicitacaoId>'. Hoje DadosManutencao (src/server/processadores/manutencao.ts) só tem lojaId. O M8 precisa aceitar esses campos, remover os objetos na hora (sem esperar os 90 dias) e atualizar lgpd_solicitacoes.resultado.objetos_removidos.
  BLOQUEIO: docs/regras-negocio.md §3.1 diz que a normalização de telefone mora em src/lib/formato.ts, mas formato.ts (fundação) só formata para exibição. A normalização ficou em src/lib/contatos/telefone.ts (normalizarTelefone, exportada por @/lib/contatos). Mover para formato.ts ou corrigir o documento; o M1 deve usar esta mesma função.
  BLOQUEIO: Semântica de consentimento não definida no documento: o que `concedido` significa para os tipos opt_out e opt_in, e qual é o texto de termo_versao. Adotei optOutDe (em src/lib/lgpd/regras.ts) e TERMO_VIGENTE = 'marketing-v1'; a tela usa só o tipo 'marketing'. O M6 precisa ler consentimentos com a mesma regra, e o texto do termo precisa de validação do Paulo ou do jurídico.
  BLOQUEIO: src/components/comum/barra-ferramentas.tsx (fundação): o termo de busca fica em estado local e volta para a URL 250 ms depois de qualquer limpeza externa, então 'Limpar filtros' não consegue limpar a busca (contornei: a busca é limpa só pelo próprio campo).
  BLOQUEIO: Pisos das travas (fundação ou integração) que o M2 permite subir: tests/componentes/block-3s.test.tsx ganha três telas ligadas — excluir-registro (contato), exportar-dossie-do-titular e eliminar-dados-do-titular — com provas em tests/componentes/contatos-telas.test.tsx; tests/seguranca/guarda.test.ts ganha 2 arquivos 'use server' (actions/contatos.ts e actions/lgpd.ts); tests/seguranca/escopo-loja.test.ts ganha consultas de domínio em src/lib/contatos e src/lib/lgpd.
  BLOQUEIO: Nenhuma rota nova foi criada, porque docs/seguranca/caminhos-de-acesso.md é da fundação e a trava T2 exige cada rota listada lá. O CSV e o dossiê saem no corpo de Server Actions (POST, sem cache) e são montados no navegador; o no-store de 02-seguranca §16 fica atendido por serem POST.

### M3 — Mídia e galeria — ok=true
commits: 8e72794 feat(midias): upload autenticado, leitura por rota interna e miniatura; 8cc8bc9 feat(galeria): grade de mídias da loja com filtros e exclusão lógica; 2f2fcdd docs(midias): documentar o módulo midias; a1e3999 test(midias): cobrir upload, leitura, ingestão, limpeza e galeria
  FALHA: npm run lint (repositório inteiro): 1 erro, em src/app/(app)/configuracoes/lojas/_components/formulario-loja.tsx:83 (M5). Os arquivos do M3 estão limpos.
  FALHA: npm run typecheck (repositório inteiro): 12 erros, todos em arquivos de outros pacotes (M1, M4, M5, M6, M2). Os arquivos do M3 não têm erro.
  FALHA: tests/seguranca/escopo-loja.test.ts (T13): 2 falhas em arquivos de outros pacotes — src/app/api/eventos/route.ts (M1) e src/lib/campanhas/pagina.ts (M6) leem o cookie loja_ativa; src/lib/integracoes/bling/cliente.ts (M4) consulta sem citar escopo.
  FALHA: Teste de sucesso do baixar-de-url está pulado (it.skipIf) até o bloqueio de ESTADOS_DE_SISTEMA ser resolvido.
  BLOQUEIO: src/lib/db/listas-fechadas.ts (fundação): falta `conversas_mensagens_midias: ["midia_id", "baixada", "url_externa"]` em ESTADOS_DE_SISTEMA. Os documentos se contradizem: 01-dados-dominio.md §2.4 diz que o job baixar-de-url faz esse UPDATE, e 01-dados.md §4.7 classifica a tabela como 'ligação pura, nunca edita'. Sem a entrada, baixarAnexo falha no atualizarEstado e a transação volta atrás. O teste já existe e liga sozinho quando a entrada entrar. Pede ADR ou correção do §4.7.
  BLOQUEIO: src/lib/db/schema/_enums/auditoria.ts (e o CHECK na migração): ACOES_AUDITADAS só tem midia_enviada e midia_excluida. A mídia recebida de cliente foi gravada como 'midia_enviada' com ator_tipo = 'sistema'; decidir se entra uma ação 'midia_recebida'.
  BLOQUEIO: Editar pasta e etiquetas na galeria (04-ui.md §5.4) não pode ser feito agora: a matriz de permissões não tem 'midia:editar' e a trilha não tem ação como 'midia_alterada'. Pela regra U8, a tela não oferece edição. Pede a chave de permissão, a ação de auditoria e a migração do CHECK.
  BLOQUEIO: Costura com M1 (repassar ao M1): guardarMidiaRecebida aceita só bytes; chamada só com url lança ErroDeIntegracao permanente. O motivo é que lojas_midias exige tamanho_bytes > 0 antes do download. Para mídia por URL, o M1 grava conversas_mensagens_midias com url_externa e midia_id nulo e chama agendarDownload({ lojaId, anexoId, provedor }), exportado de src/lib/midias/ingestao.ts. O job recebe { lojaId, anexoId, provedor }, não { lojaId, midiaId }.
  BLOQUEIO: O download por URL não envia credencial: mídia da Graph API que exige token precisa ser baixada pelo adaptador do M1 e entrar por bytes. Buscar o token no cofre ficaria dependente do formato de credencial do M5.
  BLOQUEIO: Interpretação registrada: limparMidiasExpiradas pula a mídia excluída há mais de 90 dias que ainda é referenciada por anexo vivo, para respeitar RN-M06. A anonimização LGPD remove por id pela função removerBinarios.
  BLOQUEIO: tests/componentes/block-3s.test.tsx (fundação): 'excluir-registro' da galeria deveria entrar em telasLigadas, com PISO_DE_TELAS_LIGADAS subindo para 3. A prova está em tests/componentes/midias-galeria.test.tsx.
  BLOQUEIO: tests/seguranca/escopo-loja.test.ts: PISO_DE_CONSULTAS_DE_DOMINIO continua em 0 e pode subir agora que src/lib/midias tem consultas (arquivo da fundação, não editei).
  BLOQUEIO: Contexto do worker: guard.Contexto exige autorId string e sessao. Usei `autorId: null as unknown as string` e `sessao: undefined as never` (mesmo padrão de templates/job.ts). Seria melhor um tipo oficial de contexto de sistema na fundação, porque M1, M6 e M8 vão precisar do mesmo.

### M4 — Catálogo e pedidos — ok=true
commits: 4549881 feat(catalogo): espelho do Bling somente leitura e disponibilidade calculada; 500bd28 feat(pedidos): pedido com numeração atômica, itens e ponte manual do Masc; 5dcb42d docs(pedidos): documentar o módulo pedidos; 188a35c test(pedidos): cobrir numeração, disponibilidade, Masc e cliente Bling; fe90b43 fix(pedidos): trocar valor arbitrário da altura do diálogo por utilitário
  FALHA: Lint do repositório vermelho por 2 erros do M1: react-hooks/set-state-in-effect em conversas/_components/composer.tsx:70 e tempo-real.tsx:48.
  FALHA: test:travas vermelho: T25 aponta tests/integracao/auditoria-leituras.test.ts:31 (M8).
  FALHA: test:componentes vermelho por outros pacotes: integracoes-telas.test.tsx (M5) e tokens.test.ts (auditoria, configuracoes/integracoes, relatorios, conversas, galeria).
  FALHA: tests/seguranca/escopo-loja.test.ts (T13): cookie loja_ativa lido em src/app/api/eventos/route.ts (M1) e src/lib/campanhas/pagina.ts (M6).
  FALHA: tests/seguranca/segredos.test.ts (T17): timingSafeEqual fora da porta única, em src/lib/usuarios/trocas-email.ts (M7).
  FALHA: Não houve verificação visual das telas do M4 no navegador (exige sessão com segundo fator); a cobertura é por testes de componente e integração.
  BLOQUEIO: Falta um ATOR_SISTEMA na fundação. 03-arquitetura §6.4 cita um uuid fixo semeado, que não existe no código. A sincronização grava com autorId nulo e origem 'worker': modified_by fica nulo (a FK aceita) e a trilha registra ator_tipo 'sistema' com ator_id nulo (o CHECK aceita). Quando a fundação semear o ator, basta trocar contextoDeSistema() em src/lib/catalogo/sincronizacao.ts.
  BLOQUEIO: src/lib/fila/agendamentos.ts (fundação) não agenda o job integracoes/sincronizar-bling. Hoje ele só roda se alguém o enfileirar. Sugestão: um agendamento diário ou de hora em hora, a critério do orquestrador.
  BLOQUEIO: Trilha 'antes do efeito' em cancelar e dispensar (comercial.ts e 01-dados §7.4): mutacoes.atualizarComTrava grava a trilha depois do UPDATE, na mesma transação, e não aceita motivo. O motivo entra no diff (cancelado_motivo, masc_observacao) e a falha da trilha desfaz o efeito. Gravar de fato antes exigiria mudar mutacoes.ts (fundação) para aceitar motivo e ordem.
  BLOQUEIO: Tipo da costura src/lib/catalogo/disponibilidade.ts ampliado: Disponibilidade.disponivel passou de number para number | null, e ganhou saldo, reservado e leituraAntiga. É o que 04-ui §5.2 pede ('não sabemos' quando o saldo é null). A assinatura calcularDisponivel(lojaId, sku) não mudou. Nenhum outro pacote consome a costura hoje.
  BLOQUEIO: Costuras de UI: PainelVenda virou componente de servidor assíncrono, e M1 precisa renderizá-lo a partir de um componente de servidor (dentro de Suspense). SeletorProduto ganhou o prop opcional lojaId, necessário para gestão.
  BLOQUEIO: Contadores do contato: a criação soma pedidos_contagem, pedidos_valor_total e ultima_compra_em; o cancelamento desfaz contagem e valor, por simetria com a devolução de 01-dados-dominio §6.6. A spec não fala do cancelamento; o job de reconciliação (M8) precisa usar o mesmo critério: pedidos vivos fora de cancelado e devolvido.
  BLOQUEIO: Regras em que a spec não era explícita (documentadas em docs/modulos/pedidos.md):
- pedido cancelado ou devolvido é terminal: não muda de status, não entra nem volta para o Masc;
- status escolhível na tela: confirmado, preparando, enviado, entregue;
- o limite de 'Leitura antiga' do saldo é 5 min;
- informar rastreio usa a ação de trilha pedido_status_alterado, porque a lista fechada não tem uma específica.
  BLOQUEIO: Bling: o saldo vem de GET /estoques/saldos/{idDeposito}?idsProdutos[]=..., campo saldoFisicoTotal. Conferir contra a doc vigente da API v3. As categorias do Bling não são importadas (categoria_id fica nulo).
  BLOQUEIO: Ownership: criei tests/integracao/pedidos-apoio.ts (helper, não é .test.ts), no mesmo padrão de contatos-apoio.ts e midias-apoio.ts, e os arquivos de aceite com os nomes da subsecção M4, tests/unidade/numeracao.test.ts e tests/unidade/disponibilidade.test.ts. O numeracao.test.ts usa Postgres e é pulado (skip visível) quando DATABASE_URL_TESTE não existe.

### M5 — Plataforma e integrações — ok=true
commits: 3b519d3 feat(lojas): cadastro de lojas com sigla e depósito do Bling · 4d4e237 feat(integracoes): contas por canal, cofre e telas de conexão · b5e1e0f feat(webhooks): borda de máquina com assinatura, idempotência e diário de ingestão · 934617f docs(integracoes): documentar o módulo integracoes · 9421122 test(integracoes): cobrir webhooks, OAuth do Bling, cofre, jobs e telas de conexão · 87403d6 fi…
  FALHA: Suíte do M8 (não é do M5): tests/travas/soft-delete.test.ts reprovou numa execução por tests/integracao/auditoria-leituras.test.ts:31; na execução final das travas, 48 de 48 passaram.
  FALHA: Arquivos do M1, M6 e M7 (não são do M5): tests/seguranca/escopo-loja.test.ts (T13, 'nenhum outro arquivo le o cookie loja_ativa') reprova por src/app/api/eventos/route.ts (M1), src/lib/actions/usuarios.ts (M7) e src/lib/campanhas/pagina.ts (M6).
  FALHA: Verificação visual das telas de /configuracoes no navegador não foi feita: só testes de componente e axe.
  BLOQUEIO: FUNDAÇÃO — migração quebrada: o CHECK lojas_integracoes_templates_nome em src/lib/db/migrations/0011_campanhas.sql usa '^[a-z0-9_]{1,512}$', e o Postgres recusa repetição acima de 255 ('invalid regular expression: invalid repetition count(s)'). Todo INSERT e UPDATE em lojas_integracoes_templates falha, o que quebra a sincronização de modelos (M5) e /modelos (M6). Precisa de migração 0018 com '^[a-z0-9_]+$' mais char_length(nome) <= 512, e do ajuste em src/lib/db/schema/integracoes.ts. O teste de sincronização em tests/integracao/integracoes-worker.test.ts fica pulado (it.skipIf) enquanto o CHE…
  BLOQUEIO: FUNDAÇÃO — src/lib/db/mutacoes.ts não tem registrarProcessamentoEvento (citada em 05-plano §6 e em 01-dados §6.4) nem helper de INSERT para lojas_integracoes_eventos. Implementei as duas em src/lib/integracoes/diario.ts: o INSERT por db.execute(sql…), como em auditoria/gravador.ts, e o processamento via atualizarEstado. A integração deve mover as duas para mutacoes.ts; o M1 não pode importar diario.ts sem cruzar a fronteira entre pacotes.
  BLOQUEIO: FUNDAÇÃO — ATOR_SISTEMA (03-arquitetura §6.4) não existe, nem a linha semeada em usuarios. O worker e o webhook gravam com autor nulo (modified_by nulo, ator_tipo 'sistema'), a mesma solução do M1 em src/lib/conversas/_sistema.ts. Está em src/lib/integracoes/_sistema.ts.
  BLOQUEIO: FUNDAÇÃO — ACOES_AUDITADAS não tem 'template_pausado'. Modelo pausado na Meta não é aplicado, só registrado em log.
  BLOQUEIO: FUNDAÇÃO — src/lib/fila/agendamentos.ts diverge do documento: (a) sincronizar-templates roda de hora em hora ('23 * * * *'), mas 03 §8.1 e 05 §6 pedem 30 min; (b) renovar-token roda uma vez por dia, mas o token de acesso do Bling vale 6 h, então precisa ser pelo menos de hora em hora; (c) não há agendador de sincronizar-bling. Os processadores já aceitam ser disparados sem dados e criam um job por conta.
  BLOQUEIO: FUNDAÇÃO/INTEGRAÇÃO — tests/componentes/block-3s.test.tsx: o M5 liga os itens 7 (desconectar-integracao), 8 (parear-novo-aparelho) e 9 (criar-editar-ou-desativar-loja). As provas estão em tests/componentes/integracoes-telas.test.tsx, mas o inventário e o piso de telas ligadas (PISO_DE_TELAS_LIGADAS) são da fundação. O mesmo vale para PISO_DE_CONSULTAS_DE_DOMINIO em tests/seguranca/escopo-loja.test.ts.
  BLOQUEIO: INTEGRAÇÃO — docs/seguranca/caminhos-de-acesso.md ainda marca as rotas do M5 como 'pacote M5'; os arquivos já existem. docs/integracoes.md (especificação legada) gera cerca de 20 referências quebradas no docs:check.
  BLOQUEIO: DIVERGÊNCIA — T26 (tests/travas/bling-somente-leitura.test.ts) supõe que os dois POST do OAuth ficam em src/lib/integracoes/bling/ (M4), mas o plano dá oauth.ts ao M5. Os POST ficam em src/lib/integracoes/oauth.ts, no host www.bling.com.br (está na allowlist e não bate na busca por api.bling.com.br). O M4 lê o campo access_token da credencial, compatível com o que o OAuth grava. PKCE não entrou: o Bling v3 não documenta suporte ('onde houver').
  BLOQUEIO: LIMITAÇÃO — a tela de lojas pede o depósito do Bling digitado. A lista de depósitos depende do cliente do M4, e importar de outro pacote fora das costuras é proibido.
  BLOQUEIO: NÃO CONFERIDO — os caminhos e o cabeçalho da API do uazapi (/instance/status, /instance/connect, cabeçalho 'token') vêm do padrão público e não foram conferidos no Swagger da instalação. A incerteza está concentrada em três constantes de src/lib/integracoes/uazapi.ts.

### M6 — Campanhas, conteúdo e agendadas — ok=true
commits: cf04632 feat(conteudo): respostas rápidas e modelos do WhatsApp com contagem de variáveis; 6c135de feat(campanhas): assistente, segmento com prévia e disparo em lote com lease; cf7051a feat(agendadas): mensagens programadas com gatilhos e opt-out; fcaaed0 docs(campanhas): documentar o módulo campanhas; e5ec5ed test(campanhas): cobrir disparo em lote com lease, opt-out, ritmo e agendadas
  FALHA: npm run lint vermelho por arquivo do M1 (src/app/(app)/conversas/_components/tempo-real.tsx:48); nenhum erro de lint em arquivo do M6
  FALHA: npm run typecheck vermelho por tests/integracao/pedidos-fluxo.test.ts (M4) e tests/seguranca/admin-actions.test.ts (fundação/M7); nenhum erro de tipo em arquivo do M6
  FALHA: tests/componentes/tokens.test.ts: 11 falhas em telas de outros pacotes (paleta crua ou valor arbitrário); nenhuma em tela do M6
  FALHA: 3 testes de integração de modelo PULADOS (describe.skipIf) por causa do CHECK inválido da migração 0011; voltam a rodar sozinhos quando o CHECK for corrigido
  BLOQUEIO: FUNDAÇÃO / src/lib/db/migrations/0011_campanhas.sql + src/lib/db/schema/integracoes.ts: o CHECK lojas_integracoes_templates_nome usa '^[a-z0-9_]{1,512}$', e o Postgres recusa repetição acima de 255 ('invalid repetition count(s)'). Nenhum INSERT em lojas_integracoes_templates funciona, então /modelos não grava. Correção sugerida: migração 0017 com '^[a-z0-9_]+$' AND char_length(nome) <= 512. O REGEX_NOME_MODELO em src/lib/validadores/conteudo.ts usa {1,512} e vale no JS.
  BLOQUEIO: FUNDAÇÃO / src/lib/db/schema/_enums/auditoria.ts (ACOES_AUDITADAS + CHECK): faltam ações para resposta_rapida_criada|alterada|excluida, agendamento_criado|reagendado|cancelado, template_criado|alterado|excluido e campanha_alterada. Hoje src/lib/conteudo/trilha.ts usa campanha_criada/campanha_excluida/integracao_alterada, e a coluna entidade diz a tabela. Quando a migração entrar, só esse arquivo muda.
  BLOQUEIO: FUNDAÇÃO / src/lib/db/mutacoes.ts: falta um helper de inserção em lote de campanhas_destinatarios (INSERT ... ON CONFLICT DO NOTHING com uma linha de trilha). Hoje disparo.ts usa inserirAuditado por destinatário, o que gera uma linha de auditoria por contato.
  BLOQUEIO: FUNDAÇÃO / src/lib/auth/permissoes/comercial.ts: 'campanhas:criar' está em OPERACAO (inclui vendedor), mas 05-plano §6 M6 e 04-ui §5 mandam campanhas:criar|disparar|excluir de gerente para cima. disparar e excluir já estão em GESTAO; criar não. O M6 usa a chave como está.
  BLOQUEIO: M1 / src/lib/conversas/saida.ts: lote.ts e envio.ts (commits 6c135de e cf7051a) passam o campo opcional 'modelo' que o M1 acrescentou ao EnvioParaRegistrar e ainda não commitou. O HEAD só compila quando o saida.ts do M1 entrar. A costura também não leva mídia, por isso o agendamento de mídia é recusado na validação.
  BLOQUEIO: M5 / cliente da Meta: 'Enviar para aprovação' (modelos:enviar_aprovacao) não tem costura para o M6. O botão não foi criado, para não virar fachada (U8).
  BLOQUEIO: FUNDAÇÃO / lojas_integracoes: 'ritmo configurável por integração' (03 §8.4) pede coluna que o modelo não tem; o ritmo é constante por provedor em src/lib/campanhas/regras.ts (1 uazapi, 10 oficial).
  BLOQUEIO: FUNDAÇÃO / tests/componentes/block-3s.test.tsx: as telas do M6 usam o bloqueio de 3 s ('iniciar-ou-retomar-disparo' e 'excluir-registro'), mas o inventário telasLigadas e o piso são da fundação e não foram alterados.

### M7 — Equipe e acesso — ok=true
commits: a79d14d (sequência: 8eb40e8 feat(usuarios) · 3fd7038 feat(convites) · fd7f646 docs(usuarios) · a79d14d test(usuarios))
  FALHA: Não fiz a verificação visual no navegador (PORT=3017): entrar exige conta com 2º fator no banco de dev, e a regra proíbe criar usuário com credencial fora do convite. A tela está coberta por testes de componente e pelo typecheck.
  FALHA: Falhas que não são do M7, vistas ao rodar os projetos inteiros no banco m7: tokens.test.ts (11 casos, telas de M1/M4/M5/M8); escopo-loja T13, leitura do cookie loja_ativa em src/app/api/eventos/route.ts e src/lib/campanhas/pagina.ts; integracoes-contas (M5); integridade-trilha, consentimentos órfãos; lint de tempo-real.tsx (M1) numa rodada intermediária.
  BLOQUEIO: tests/componentes/block-3s.test.tsx (fundação): a lista telasLigadas precisa ganhar convidar-usuario, trocar-papel, promover-a-admin, transferir-posse, desativar-ou-reativar-usuario, iniciar-reset-de-acesso e recuperacao-assistida, e PISO_DE_TELAS_LIGADAS precisa subir. As provas estão em tests/componentes/usuarios-tela.test.tsx.
  BLOQUEIO: src/app/(app)/perfil/** (fundação): falta o campo para a própria pessoa digitar o código da troca de e-mail e chamar confirmarMeuNovoEmail (action pronta em src/lib/actions/usuarios.ts). Sem ele a troca iniciada pelo admin não tem como ser concluída pela tela.
  BLOQUEIO: src/lib/auth/emails.ts e src/server/processadores/emails.ts (fundação): AssuntoDeSeguranca não tem assunto para o código de troca de e-mail nem para o aviso de troca solicitada. Por ora o código vai no fragmento do link (APP_URL/perfil#codigo=NNNNNN) com o assunto 'email-trocado', que tem texto enganoso para o endereço novo.
  BLOQUEIO: src/lib/db/schema/auth-eventos.ts, DetalhesAuthEvento (fundação): a lista de campos permitidos não tem ciencia_versao nem motivo de recusa. Registrei a ciência como detalhes.acao='ciencia:CIENCIA_ADMIN_V1' e a recusa de alvo como detalhes.rota='ALVO_NAO_PERMITIDO'. A especificação (§2.3) pede o código 403 ALVO_NAO_PERMITIDO, mas o ErroDeAlvo da fundação responde SEM_PERMISSAO, e o contrato fechado de códigos da UI (04 §7.2) não tem ALVO_NAO_PERMITIDO; segui a fundação.
  BLOQUEIO: src/lib/auth/convites.ts (fundação): emitirConvite abre a própria transação e não serve para reenviar numa transação só (o índice único parcial de e-mail aberto travaria as duas). Compus a emissão em src/lib/usuarios/convites.ts reusando novoToken, linkDoConvite e VALIDADE_CONVITE_HORAS. Sugestão: expor na fundação uma variante que receba a transação, para ter uma emissão só.
  BLOQUEIO: Recuperação assistida (§9.3) exige remover os fatores, e isso é delete físico feito pela biblioteca (tabelas compliance:framework). Usei (await auth.$context).adapter.delete para passkey e twoFactor dentro de src/lib/usuarios/acesso.ts. Passa no auditor e nas travas, mas o lugar natural seria uma função da fundação em src/lib/auth/fatores.ts (ex.: removerTodosOsFatores). Peço revisão do dono de auth.
  BLOQUEIO: usuarios_convites não tem coluna nome: o formulário de convite pede só e-mail, papel, loja, ciência e motivo; o nome é digitado no primeiro acesso. 04-ui.md §5.6 lista 'nome' no convite; divergência registrada, sem coluna nova.
  BLOQUEIO: A expiração de convite não fecha o índice único parcial (usado_em IS NULL AND is_deleted = false): convite vencido segura o e-mail até alguém aposentá-lo. Tratei isso no convidar (aposenta o vencido antes de emitir); o job expirar-convites (M8) deveria fazer o mesmo.
  BLOQUEIO: tests/seguranca/_apoio.ts limparAuth (fundação): TRUNCATE usuarios CASCADE apaga contatos (FK de modified_by) e deixa consentimentos órfãos quando a suíte inteira roda num banco só. Por isso integridade-trilha falhou no banco m7; isso depende da ordem em que as suítes rodam.

### M8 — Alertas, auditoria e relatórios — ok=true
commits: e88d181 feat(alertas): geração, reconhecimento e resolução pelo gerador; c3d4338 feat(auditoria): trilha, qualidade, excluídos e trilha de acesso; a4e1467 feat(relatorios): indicadores por loja e período com definição explícita; d1d78ae docs(auditoria): documentar o módulo auditoria; 803ec97 test(auditoria): cobrir indicadores, reconciliação e telas de alertas e relatórios; 29c7142 fix(auditoria):…
  FALHA: Gravação real de alertas indisponível (ver bloqueio 1). Até a fundação entregar, o job gerar-alertas falha com NAO_IMPLEMENTADO depois de detectar e de sincronizar sla_estourado_em, e vai para a DLQ; o botão Reconhecer está escondido.
  FALHA: A reconciliação não gera alerta: falta um tipo de alerta para divergência (bloqueio 2). A divergência sai como log error com os ids.
  FALHA: A retenção não zera lojas_integracoes_eventos.ip (bloqueio 3).
  FALHA: O job expirar-convites só conta e avisa, sem liberar o e-mail do convite vencido (bloqueio 4).
  FALHA: Telas não verificadas no navegador: não subi next dev.
  BLOQUEIO: 1. src/lib/db/mutacoes.ts e src/lib/db/listas-fechadas.ts (+ migração do CHECK de ACOES_AUDITADAS): alertas precisa de (a) helper abrirAlerta(tx, novo) com INSERT ... ON CONFLICT (loja_id, chave_deduplicacao) WHERE resolvido_em IS NULL AND is_deleted = false DO NOTHING RETURNING id, sem trilha e com modified_by nulo; (b) 'alertas': ['resolvido_em'] em ESTADOS_DE_SISTEMA, para usar atualizarEstado; (c) ação 'alerta_reconhecido' em ACOES_AUDITADAS, para atualizarComTrava. Depois disso, trocar só src/lib/alertas/escrita.ts e ligar ESCRITA_DISPONIVEL. A forma exata está em tests/integracao/auditor…
  BLOQUEIO: 2. src/lib/db/schema/_enums/plataforma.ts (TIPOS_ALERTA) + migração do CHECK alertas_tipo_lista: não há tipo para 'espelho divergente', que a reconciliação precisa para gerar alerta (01-dados-dominio.md §7.2 e 03 §8.1).
  BLOQUEIO: 3. src/lib/db/listas-fechadas.ts: ESTADOS_DE_SISTEMA.lojas_integracoes_eventos não inclui 'ip'; 01-dados.md §6.4 manda zerar o ip na retenção de 30 dias.
  BLOQUEIO: 4. src/lib/auth/convites.ts: o convite vencido já não é aceito, mas continua segurando o índice uq_usuarios_convites_email_aberto e impede um novo convite para o mesmo e-mail. Falta uma função da fundação que feche o vencido (soft delete) e uma ação auditada que a descreva; até lá o job só conta.
  BLOQUEIO: 5. src/lib/lgpd (M2): não há gravação exposta para lgpd_solicitacoes.resultado.objetos_removidos depois do limpar-midia da LGPD; o valor sai só no log.
  BLOQUEIO: 6. src/app/globals.css: os tokens --chart-1..5 citados em 04-ui.md §3 não existem; o gráfico usa var(--chart-n, var(--primary)).
  BLOQUEIO: 7. Divergência de chave na especificação: 04-ui.md §5.5 e src/lib/navegacao.ts usam pedidos:ler para /relatorios, enquanto docs/seguranca/caminhos-de-acesso.md e a matriz usam relatorios:ler. Usei relatorios:ler (ambas valem para TODOS os papéis) e relatorios:exportar para o CSV.
  BLOQUEIO: 8. Observação para a fundação e a integração: as páginas não podem ler o cookie loja_ativa (T13); por isso as telas do M8 buscam os dados por Server Action (executarAcao resolve a loja escolhida). Os pisos PISO_DE_ACTIONS e PISO_DE_CONSULTAS_DE_DOMINIO (tests/seguranca) continuam na fundação e não foram subidos.
